/target
**/*.rs.bk
test.*
*.txt
domains
*.csv
findomain.toml
configtest.toml
/screenshots
/sshots
# Right now ARM builds fails on the first attempt, so we need to retry...
armbuilder.sh

# Binaries staged for the Docker build, never committed.
docker/bin/
